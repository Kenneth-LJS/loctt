# LocTT TODO

Tracked items for the next phase of development. Clean up before release.

## Active work

(Empty — the migration that this file used to track is complete. `apps/web`, attachments upload, activity log, relationship ranking, reorder API, archived-reference guard, and history-with-actor are all landed. Add items here as new work surfaces.)

## Carrying forward (informational)

The frontend (`apps/web`) was originally prototyped in a separate repo and folded in under `apps/web/` once feature-complete. The web app shares the same `.loctt/` data directory as the CLI / MCP server — no separate database.
