// @loctt/service — local service layer
// Entry point for the LocTT service that the web app talks to.
// Implementation will be added in Task 37.

export function createService(): { started: false } {
  return { started: false };
}
