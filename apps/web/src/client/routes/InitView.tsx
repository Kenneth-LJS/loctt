import { Placeholder } from "./Placeholder.tsx";

export function InitView() {
  return (
    <Placeholder
      title="Init wizard"
      subtitle="Phase 6 — first-run flow (project name + prefix, skip docs, defaults)."
    />
  );
}
