// @loctt/mcp — MCP server
// Entry point for the LocTT MCP server.
// Tool implementations will be added in Task 39.

export function createMcpServer(): { started: false } {
  return { started: false };
}
